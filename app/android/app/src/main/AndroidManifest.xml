<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <!-- La narración en vivo del padre es funcionalidad destacada, no ruido de fondo.
         Ver docs/plan-de-trabajo.md, hallazgo 1. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Solo hasta API 28. Desde API 29 el almacenamiento por ámbito deja escribir en
         MediaStore sin permiso alguno, y pedirlo ahí sería pedir de más: en una app
         para padres con la cámara apuntando a un niño, cada permiso extra es fricción
         en la instalación y una pregunta más en la revisión de la tienda. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <!-- required="false": la app funciona sin ARCore, con colocación por toque. Es la
         degradación de la decisión B; con "true" Play Store la ocultaría en todos los
         dispositivos no certificados, que es parte del parque objetivo. -->
    <uses-feature android:name="android.hardware.camera.ar" android:required="false" />

    <application
        android:label="Ratón Pérez"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- "optional", no "required", por el mismo motivo. -->
        <meta-data android:name="com.google.ar.core" android:value="optional" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
